# Final confirmation audit B

| Record | Verdict | Repairs confirmed | Source evidence and regression check | Required action |
|---|---|---|---|---|
| CNF-P011 | PASS | `d4.allocation_structure` is `NOT_APPLICABLE`. `td1` keeps participant-level random assignment of the two-measure task. | Methods, “Procedure,” states that each resident completed two of four instruments and that the instruments were randomly assigned. Results, opening paragraph, reports 24 residents and 12 interviews per measure. The final record keeps the supported instruments, methods, findings, limitations, and exact source labels. | None. |
| CNF-P012 | PASS | `mu7` and `mu6` are absent. `su1` keeps the LSS scoring rule. `mod1` and `mod2` keep the two regression uses, and `l8.about` links to both models without duplicate IDs. | Methods, “Data collection and outcome measures,” defines the LSS as the five-level sum from 5 to 25 and calls it the primary outcome. Methods, “Statistical analysis,” specifies separate multiple linear regressions for LSS and EQ VAS. Results, “Study population,” gives 19,432 completed, 1,825 excluded, and 17,607 analyzed respondents. The inequality results and both source conflicts remain intact. | None. |
| CNF-P013 | MINOR | `sam5` and `iu3` are absent. `du3` keeps the APERSU aggregate comparator, and `f7.about` uses `du3`. The record adds the 2,199-patient source flow and the 25 same-day exclusions. `sam7` is linked to both repeated-measure findings. | Methods, “Design and Setting,” paragraph 1, states that 2,199 adult EOPCN patients had data drawn from electronic medical records and that 25 same-day pre/post records were excluded. It does not report enrollment. `sam6` therefore invents an `ENROLLED` stage and changes the counting unit from patients to patient records. Methods, “Data Analysis,” final paragraph, and Table 2 support the APERSU aggregate comparison. The program counts and results remain source-faithful. | Change `sam6.unit` to `patients`. Do not use `ENROLLED`. Use an `UNMAPPED_VALUE` sample-stage gap for the source-record-availability stage, or keep the exact 2,199-patient fact outside a staged `Sample`. This is an ontology gap plus a local mapping error, not a scientific-result error. |
| CNF-P014 | PASS | `a1.applies_to` contains only `iu1`. `a2.applies_to` contains only `iu2` and `iu3`. Neither administration refers to `su1` or `su2`. | Methods, “Data and instruments,” states that EQ-5D-3L measures current health and that MSIS-8D is derived from MSIS-29v2 responses about the past two weeks. Results, “Acceptability” and “Responsiveness,” support 3.14% versus 8.57% missing data and the −0.047 relapse change for both measures. No scientific or structural regression was found. | None. |
| CNF-P015 | PASS | `pu1` is absent. `du2` and `su2` keep the original Dutch EQ-5D-5L values only as comparator input. | Methods, “Analysis,” first paragraph, explicitly states that the study did not use TTO or preferences for dying immediately. Results, “Demographics,” reports 385 completed participants, five exclusions, and 380 analyzed participants. The PC/BWS tasks, model comparisons, pits-scale product, findings, limitations, and source conflict remain intact. | None. |
| CNF-P016 | PASS | `d3` is `UNMAPPED_VALUE`, and `g1` records the response-scale comparison gap. `td1.applies_to` contains only `iu3`, `iu4`, and `iu2`. `mu7` and `mu8` keep the two source-study quality controls. `lim1.about` and `lim3.about` contain no Study ID. Registry repairs split `iu1` into exact `EQ-HWB` and `EQ-HWB-S` uses, split `mu2` into exact Shannon index uses, and replace `mu5` references with `mod2`; no stale ID remains. | Methods, “Data collection,” reports randomized instrument order, attention checks, and minimum completion-time thresholds. “Measures—EQ-HWB/EQ-HWB-S” names both instruments and four pain/discomfort items. Methods, “Analysis,” separately names Shannon’s Index, Shannon’s Evenness Index, GRM, DIF, and four ordinal logistic models. Results, Tables 2–6, support the retained correlations, informativity, discrimination, DIF, and odds ratios. No regression or invented fact was found. | None. |

## Totals and registry check

- PASS: 5
- MINOR: 1
- MAJOR: 0
- All requested deletions, type changes, splits, cross-reference changes, and registry-record repairs are present.
- No deliberate null registry mapping remains in these six normalized records. The repaired source labels remain exact, including `EQ-HWB`, `EQ-HWB-S`, `Shannon’s Index (H’)`, and `Shannon’s Evenness Index (J’)`.
- Apart from the local CNF-P013 sample-stage and counting-unit issue, I found no scientific regression, unsupported result, or invented fact.
