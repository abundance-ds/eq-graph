# Invalidated run

This run stopped after a source-audit smoke test found a repeated structural
omission: the record had no exact type for software or computational platforms.
The corpus contains frequent software and EQ-VT references. Do not load or
combine records from this run with the replacement run.

The stopped directory remains only as process evidence. The replacement task,
schema, prepared inputs, and run use `SoftwareUse`.
