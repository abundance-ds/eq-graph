# eq-graph backend — deployment configuration

ktor:
  deployment:
    port: 8080
  application:
    modules:
      - rs.shoulde.eqgraph.ServerKt.server

neo4j:
  uri: ${NEO4J_URI}
  user: ${NEO4J_USER}
  password: ${NEO4J_PASSWORD}
  maxConcurrentSessions: 90
